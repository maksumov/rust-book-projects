// Chapter 11 playground: each section of the chapter lives in its
// own module (see adder.rs, rectangle.rs, ...). Verified with
// `cargo test`, not `cargo run` -- a lib has no binary.

pub mod adder;
pub mod greeter;
pub mod rectangle;
